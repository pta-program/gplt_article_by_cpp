# L1-002 - 打印沙漏（20 分）

- **时间限制**: 400 ms
- **内存限制**: 65536 KB
- **代码长度限制**: 16 KB

---

## 题目描述

本题要求你写个程序把给定的符号打印成沙漏的形状。例如给定17个“*”，要求按下列格式打印
```
*****
 ***
  *
 ***
*****
```

所谓“沙漏形状”，是指每行输出奇数个符号；各行符号中心对齐；相邻两行符号数差2；符号数先从大到小顺序递减到1，再从小到大顺序递增；首尾符号数相等。

给定任意N个符号，不一定能正好组成一个沙漏。要求打印出的沙漏能用掉尽可能多的符号。

### 输入格式:

输入在一行给出1个正整数N（$$\le$$1000）和一个符号，中间以空格分隔。

### 输出格式:

首先打印出由给定符号组成的最大的沙漏形状，最后在一行中输出剩下没用掉的符号数。

### 输入样例:
```in
19 *
```

### 输出样例:
```out
*****
 ***
  *
 ***
*****
2
```

## 示例

### 示例 1

**输入:**
```
19 *
```

**输出:**
```
*****
 ***
  *
 ***
*****
2
```

---

## 题目详解

### 一、解题思路

沙漏由两部分组成：上半部分符号数从大到小依次递减（步长为 2），中间为 1 个符号，下半部分从小到大依次递增。

设沙漏的层数为 `n`（从中间向上下两侧的层数，最宽层有 `2n-1` 个符号），则整个沙漏的符号总数为：

```
总符号数 = 2 × (1 + 3 + 5 + ... + (2n-1)) - 1 = 2n² - 1
```

解题步骤：

1. **求最大层数**：找到最大的 `n`，使得 `2n² - 1 ≤ N`（即用掉尽可能多的符号）。
2. **求剩余符号数**：`剩余 = N - (2n² - 1)`。
3. **打印上半部分**：行号 `i` 从 `n` 递减到 `1`，每行先打印 `n - i` 个空格（居中对齐），再打印 `2i - 1` 个符号。
4. **打印下半部分**：行号 `i` 从 `2` 递增到 `n`，每行同样先打印 `n - i` 个空格，再打印 `2i - 1` 个符号。
5. **输出剩余符号数**。

### 二、代码流程说明

1. 读取整数 `N` 和字符 `symbol`。
2. 通过 `while` 循环递增 `max_level`，判断条件 `2 * (max_level + 1)² - 1 <= N`，求出能组成完整沙漏的最大层数 `max_level`。
3. **打印上半部分**：外层循环 `i` 从 `max_level` 递减到 1；内层第一个循环打印 `max_level - i` 个空格实现居中对齐，第二个循环打印 `2 * i - 1` 个符号，随后换行。
4. **打印下半部分**：外层循环 `i` 从 2 递增到 `max_level`；每行同样先打印 `max_level - i` 个空格，再打印 `2 * i - 1` 个符号并换行。
5. 输出剩余符号数 `N - (2 * max_level * max_level - 1)` 并结束。

### 三、代码流程图

```mermaid
flowchart TD
    A([开始]) --> B[读取整数 N 和符号 symbol]
    B --> C[初始化 max_level = 0]
    C --> D{2 * (max_level+1)² - 1 <= N ?}
    D -- 是 --> E[max_level 加 1]
    E --> D
    D -- 否 --> F[打印上半部分：i 从 max_level 递减到 1]
    F --> G[先打印 max_level - i 个空格]
    G --> H[再打印 2i - 1 个符号并换行]
    H --> I{该行所有 i 是否已处理完？}
    I -- 否 --> G
    I -- 是 --> J[打印下半部分：i 从 2 递增到 max_level]
    J --> K[先打印 max_level - i 个空格]
    K --> L[再打印 2i - 1 个符号并换行]
    L --> M{该行所有 i 是否已处理完？}
    M -- 否 --> K
    M -- 是 --> N[输出剩余符号数 N - (2 * max_level² - 1)]
    N --> O([结束])
```

### 四、解题流程图

```mermaid
flowchart TD
    A([开始]) --> B[输入 N 个符号和符号字符]
    B --> C[由公式 2n² - 1 确定最大层数 n，使 2n² - 1 ≤ N]
    C --> D[计算剩余符号数：N - (2n² - 1)]
    D --> E[打印沙漏上半部分：符号数从 2n-1 递减到 1]
    E --> F[打印沙漏下半部分：符号数从 3 递增到 2n-1]
    F --> G[输出剩余符号数]
    G --> H([结束])
```
