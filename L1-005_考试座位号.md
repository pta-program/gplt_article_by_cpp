# L1-005 - 考试座位号（15 分）

- **时间限制**: 200 ms
- **内存限制**: 65536 KB
- **代码长度限制**: 16 KB

---

## 题目描述

每个 PAT 考生在参加考试时都会被分配两个座位号，一个是试机座位，一个是考试座位。正常情况下，考生在入场时先得到试机座位号码，入座进入试机状态后，系统会显示该考生的考试座位号码，考试时考生需要换到考试座位就座。但有些考生迟到了，试机已经结束，他们只能拿着领到的试机座位号码求助于你，从后台查出他们的考试座位号码。

### 输入格式:

输入第一行给出一个正整数 $$N$$（$$\le 1000$$），随后 $$N$$ 行，每行给出一个考生的信息：`准考证号 试机座位号 考试座位号`。其中`准考证号`由 16 位数字组成，座位从 1 到 $$N$$ 编号。输入保证每个人的准考证号都不同，并且任何时候都不会把两个人分配到同一个座位上。

考生信息之后，给出一个正整数 $$M$$（$$\le N$$），随后一行中给出 $$M$$ 个待查询的试机座位号码，以空格分隔。

### 输出格式:

对应每个需要查询的试机座位号码，在一行中输出对应考生的准考证号和考试座位号码，中间用 1 个空格分隔。

### 输入样例:
```in
4
3310120150912233 2 4
3310120150912119 4 1
3310120150912126 1 3
3310120150912002 3 2
2
3 4
```

### 输出样例:
```out
3310120150912002 2
3310120150912119 1
```

## 示例

### 示例 1

**输入:**
```
4
3310120150912233 2 4
3310120150912119 4 1
3310120150912126 1 3
3310120150912002 3 2
2
3 4
```

**输出:**
```
3310120150912002 2
3310120150912119 1
```

---

## 题目详解

### 一、解题思路

题目保证试机座位号从 1 到 N 编号且互不重复，因此可以**直接用试机座位号作为数组下标**建立映射表，实现 O(1) 查询。

算法步骤：

1. 读入考生人数 `N`。
2. 建立两个数组：`id[]` 存准考证号（字符串）、`exam_seat[]` 存考试座位号，下标即试机座位号。
3. 读入 N 个考生的 `准考证号、试机座位号、考试座位号`，按试机座位号写入数组。
4. 读入查询个数 `M`，对每个待查询的试机座位号，直接按下标取出准考证号和考试座位号并输出。

### 二、代码流程说明

1. 读入考生人数 `N`。
2. 动态分配数组 `string* id = new string[N + 1]` 和 `int* exam_seat = new int[N + 1]`（下标对应试机座位号）。
3. 循环 `i` 从 0 到 N-1：
   - 读入 `student_id`（准考证号）、`test_seat`（试机座位号）、`ex_seat`（考试座位号）；
   - 存入 `id[test_seat] = student_id;`、`exam_seat[test_seat] = ex_seat;`。
4. 读入查询个数 `M`。
5. 循环 `i` 从 0 到 M-1：读入 `query_seat`，输出 `id[query_seat]` 与 `exam_seat[query_seat]` 并换行。
6. 释放动态数组内存 `delete[]`，`return 0` 结束程序。

### 三、代码流程图

```mermaid
flowchart TD
    A([开始]) --> B[读入考生人数 N]
    B --> C[动态分配数组 id 与 exam_seat]
    C --> D[循环读入 N 个考生信息]
    D --> E[以试机座位号 test_seat 为下标写入 id 和 exam_seat]
    E --> F{i 小于 N?}
    F -- 是 --> D
    F -- 否 --> G[读入查询个数 M]
    G --> H[循环读入 M 个待查询试机座位号]
    H --> I[按下标输出 id[query_seat] 和 exam_seat[query_seat]]
    I --> J{i 小于 M?}
    J -- 是 --> H
    J -- 否 --> K[释放动态数组内存]
    K --> L([结束])
```

### 四、解题流程图

```mermaid
flowchart TD
    A([开始]) --> B[输入 N 个考生的准考证号、试机座位号、考试座位号]
    B --> C[以试机座位号为下标建立考生信息映射表]
    C --> D[输入 M 个待查询的试机座位号]
    D --> E[逐个按下标 O(1) 查询并输出准考证号与考试座位号]
    E --> F([结束])
```
