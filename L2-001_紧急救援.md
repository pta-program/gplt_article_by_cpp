# L2-001 - 紧急救援（25 分）

- **时间限制**: 200 ms
- **内存限制**: 65536 KB
- **代码长度限制**: 16 KB

---

## 题目描述

作为一个城市的应急救援队伍的负责人，你有一张特殊的全国地图。在地图上显示有多个分散的城市和一些连接城市的快速道路。每个城市的救援队数量和每一条连接两个城市的快速道路长度都标在地图上。当其他城市有紧急求助电话给你的时候，你的任务是带领你的救援队尽快赶往事发地，同时，一路上召集尽可能多的救援队。

### 输入格式:

输入第一行给出4个正整数$$N$$、$$M$$、$$S$$、$$D$$，其中$$N$$（$$2\le N\le 500$$）是城市的个数，顺便假设城市的编号为0 ~ $$(N-1)$$；$$M$$是快速道路的条数；$$S$$是出发地的城市编号；$$D$$是目的地的城市编号。

第二行给出$$N$$个正整数，其中第$$i$$个数是第$$i$$个城市的救援队的数目，数字间以空格分隔。随后的$$M$$行中，每行给出一条快速道路的信息，分别是：城市1、城市2、快速道路的长度，中间用空格分开，数字均为整数且不超过500。输入保证救援可行且最优解唯一。

### 输出格式:

第一行输出最短路径的条数和能够召集的最多的救援队数量。第二行输出从$$S$$到$$D$$的路径中经过的城市编号。数字间以空格分隔，输出结尾不能有多余空格。

### 输入样例:
```in
4 5 0 3
20 30 40 10
0 1 1
1 3 2
0 3 3
0 2 2
2 3 2
```

### 输出样例:
```out
2 60
0 1 3
```

## 示例

### 示例 1

**输入:**
```
4 5 0 3
20 30 40 10
0 1 1
1 3 2
0 3 3
0 2 2
2 3 2
```

**输出:**
```
2 60
0 1 3
```

---

## 题目详解

### 一、解题思路

本题是带附加条件的单源最短路径问题，使用 Dijkstra 算法求解。在求出最短距离的同时，还要维护三样附加信息：最短路径的条数（`count_path`）、沿途能召集的最多救援队数量（`max_rescue`）以及最短路径本身（通过前驱数组 `prev_node` 记录）。算法流程如下：

1. 用邻接表 `graph` 存储无向图，城市救援队数量存入 `rescue_team` 数组。
2. 初始化：所有节点的 `dist` 为无穷大 `INF`，`count_path` 为 0，`max_rescue` 为 0，`prev_node` 为 -1；起点 `S` 的 `dist` 为 0，`count_path` 为 1，`max_rescue` 为 `rescue_team[S]`。
3. 循环 N 次执行 Dijkstra 核心步骤：每次在所有未访问节点中选出距离最小的节点 `u`（若无可选则跳出），标记 `visited[u] = true`，再松弛它的所有邻边。
4. 松弛时对每条边 `(u, v, w)` 分两种情况更新：
   - 若 `dist[v] > dist[u] + w`，说明找到更短路径：更新距离、路径条数继承为 `count_path[u]`、救援队数量更新为 `max_rescue[u] + rescue_team[v]`，并记录前驱 `prev_node[v] = u`。
   - 若 `dist[v] == dist[u] + w`，说明路径长度相同：路径条数累加 `count_path[v] += count_path[u]`；若这条同长路径能召集更多救援队，则更新 `max_rescue[v]` 并改写前驱。
5. 输出 `count_path[D]` 和 `max_rescue[D]`，再从终点 `D` 沿 `prev_node` 回溯到 -1，反转后得到完整路径并输出。

### 二、代码流程说明

1. 读入 `N`、`M`、`S`、`D`，循环读入 `N` 个城市的救援队数量存入 `rescue_team`。
2. 循环读入 `M` 条道路，把无向边分别加入 `graph[a]` 与 `graph[b]` 的邻接表。
3. 初始化各数组：`dist` 全部置为 `INF`，`count_path`、`max_rescue` 置 0，`prev_node` 置 -1，`visited` 置 false。
4. 设置起点：`dist[S] = 0`，`count_path[S] = 1`，`max_rescue[S] = rescue_team[S]`。
5. 外层 `for (int i = 0; i < N; i++)` 循环执行 Dijkstra 选择：内层遍历找未访问且距离最小的节点 `u`，记录其距离 `min_dist`；若 `u == -1` 则 `break` 跳出，否则 `visited[u] = true`。
6. 遍历 `u` 的所有邻边 `e`，取出 `v = e.to`、`w = e.length`；若 `v` 未访问：
   - `dist[v] > dist[u] + w` 时，更新距离、条数、救援队数、前驱；
   - `dist[v] == dist[u] + w` 时，条数累加，若救援队数更多则更新救援队数与前驱。
7. 输出第一行：`count_path[D]` 与 `max_rescue[D]`。
8. 从 `curr = D` 开始沿 `prev_node` 循环回溯，把节点依次压入 `path`，直到 `curr == -1`。
9. 用 `reverse` 反转 `path`，用空格分隔输出全部城市编号并换行，最后 `return 0`。

### 三、代码流程图

```mermaid
flowchart TD
    A([开始]) --> B["读入 N M S D"]
    B --> C["读入各城市救援队数量 rescue_team"]
    C --> D["读入 M 条道路构建邻接表 graph"]
    D --> E["初始化 dist count_path max_rescue prev_node visited"]
    E --> F["设置起点 dist[S]=0 count_path[S]=1 max_rescue[S]=rescue_team[S]"]
    F --> G["外层循环 i 从 0 到 N-1"]
    G --> H["遍历未访问节点找距离最小的 u"]
    H --> I{"u 是否存在?"}
    I -- 否 --> J["break 跳出外层循环"]
    I -- 是 --> K["visited[u] = true"]
    K --> L["遍历 u 的每条邻边 e"]
    L --> M{"v 已访问?"}
    M -- 是 --> N["跳过该边"]
    M -- 否 --> O{"dist[v] > dist[u]+w?"}
    O -- 是 --> P["更新 dist[v] count_path[v] max_rescue[v] prev_node[v]"]
    P --> N
    O -- 否 --> Q{"dist[v] == dist[u]+w?"}
    Q -- 是 --> R["count_path[v] += count_path[u]"]
    R --> S{"max_rescue[v] < max_rescue[u]+rescue_team[v]?"}
    S -- 是 --> T["更新 max_rescue[v] 与 prev_node[v]"]
    S -- 否 --> N
    T --> N
    Q -- 否 --> N
    N --> L
    L --> U{"邻边遍历完?"}
    U -- 否 --> L
    U -- 是 --> G
    G --> V["外层循环结束"]
    V --> W["输出 count_path[D] 和 max_rescue[D]"]
    W --> X["从 D 沿 prev_node 回溯到 -1 得到 path"]
    X --> Y["reverse 反转 path"]
    Y --> Z["输出路径城市编号"]
    Z --> AA([结束])
