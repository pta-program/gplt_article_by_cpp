# L2-025 - 分而治之（25 分）

- **时间限制**: 600 ms
- **内存限制**: 65536 KB
- **代码长度限制**: 16 KB

---

## 题目描述

分而治之，各个击破是兵家常用的策略之一。在战争中，我们希望首先攻下敌方的部分城市，使其剩余的城市变成孤立无援，然后再分头各个击破。为此参谋部提供了若干打击方案。本题就请你编写程序，判断每个方案的可行性。

### 输入格式:

输入在第一行给出两个正整数 N 和 M（均不超过10 000），分别为敌方城市个数（于是默认城市从 1 到 N 编号）和连接两城市的通路条数。随后 M 行，每行给出一条通路所连接的两个城市的编号，其间以一个空格分隔。在城市信息之后给出参谋部的系列方案，即一个正整数 K （$$\le$$ 100）和随后的 K 行方案，每行按以下格式给出：

```
Np v[1] v[2] ... v[Np]
```

其中 `Np` 是该方案中计划攻下的城市数量，后面的系列 `v[i]` 是计划攻下的城市编号。

### 输出格式:

对每一套方案，如果可行就输出`YES`，否则输出`NO`。

### 输入样例:
```in
10 11
8 7
6 8
4 5
8 4
8 1
1 2
1 4
9 8
9 1
1 10
2 4
5
4 10 3 8 4
6 6 1 7 5 4 9
3 1 8 4
2 2 8
7 9 8 7 6 5 4 2
```

### 输出样例:
```out
NO
YES
YES
NO
NO
```

## 示例

### 示例 1

**输入:**
```
10 11
8 7
6 8
4 5
8 4
8 1
1 2
1 4
9 8
9 1
1 10
2 4
5
4 10 3 8 4
6 6 1 7 5 4 9
3 1 8 4
2 2 8
7 9 8 7 6 5 4 2
```

**输出:**
```
NO
YES
YES
NO
NO
```

---

## 题目详解

### 一、解题思路

本题判断攻击方案是否可行：攻击后剩余城市之间必须没有通路（即每条边至少有一个端点被攻占）。

1. **方案可行条件**：对于图中所有边 `(u, v)`，`u` 和 `v` 中至少有一个在攻击列表中。
2. **算法**：
   - 用 `unordered_set` 存储每个方案中被攻击的城市
   - 遍历所有边，检查每条边的两个端点是否至少有一个在攻击集合中
   - 若所有边都满足条件，方案可行；否则不可行

### 二、代码流程说明

1. 读取城市数 `N` 和道路数 `M`
2. 读取 `M` 条边存入 `edges` 向量
3. 读取方案数 `K`
4. 对每个方案：
   - 读取攻击城市数 `Np` 和城市列表，存入 `attacked` 集合
   - 初始化 `feasible = true`
   - 遍历所有边：
     - 若边的两个端点都不在 `attacked` 中，`feasible = false` 并 break
   - 输出 `YES` 或 `NO`

### 三、代码流程图

```mermaid
flowchart TD
    A([开始]) --> B[/读取 N M/]
    B --> C[/读取 M 条边/]
    C --> D[/读取 K 个方案/]
    D --> E{遍历 K 个方案}
    E --> F[/读取 Np 个攻击城市/]
    F --> G[存入 attacked 集合]
    G --> H[feasible = true]
    H --> I{遍历所有边}
    I --> J{两端都不在 attacked?}
    J -- 是 --> K[feasible = false<br>break]
    J -- 否 --> I
    I -- 完成 --> L[输出 YES 或 NO]
    K --> L
    L --> E
    E -- 完成 --> M([结束])
```

### 四、解题流程图

```mermaid
flowchart TD
    A([开始]) --> B[理解题意：攻击后剩余城市必须孤立]
    B --> C[条件：每条边至少一端被攻击]
    C --> D[用集合存储攻击城市]
    D --> E[遍历所有边检查端点]
    E --> F{所有边至少一端被攻击?}
    F -- 是 --> G[YES]
    F -- 否 --> H[NO]
    G --> I([结束])
    H --> I
```
