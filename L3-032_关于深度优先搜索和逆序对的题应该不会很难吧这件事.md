# L3-032 - 关于深度优先搜索和逆序对的题应该不会很难吧这件事（30 分）

- **时间限制**: 1000 ms
- **内存限制**: 262144 KB
- **代码长度限制**: 16 KB

---

## 题目描述

## 背景知识

### 深度优先搜索与 DFS 序

深度优先搜索算法（DFS）是一种用于遍历或搜索树或图的算法。以下伪代码描述了在树 $T$ 上进行深度优先搜索的过程：

```
procedure DFS(T, u, L)      // T 是被深度优先搜索的树
                            // u 是当前搜索的节点
                            // L 是一个链表，保存了所有节点被第一次访问的顺序
  append u to L             // 将节点 u 添加到链表 L 的末尾
  for v in u.children do    // 枚举节点 u 的所有子节点 v
    DFS(T, v)               // 递归搜索节点 v
```

令 $r$ 为树 $T$ 的根，调用 `DFS(T, r, L)` 即可完成对 $T$ 的深度优先搜索，保存在链表 $L$ 中的排列被称为 DFS 序。相信聪明的你已经发现了，如果枚举子节点的顺序不同，最终得到的 DFS 序也会不同。

### 逆序对

给定一个长度为 $n$ 的整数序列 $a_1, a_2, \cdots, a_n$，该序列的逆序对数量是同时满足以下条件的有序数对 $(i, j)$ 的数量：
* $1 \le i < j \le n$；
* $a_i > a_j$。

## 问题求解

给定一棵 $n$ 个节点的树，其中节点 $r$ 为根。求该树所有可能的 DFS 序中逆序对数量之和。

## 输入格式

第一行输入两个整数 $n$，$r$（$2 \le n \le 3 \times 10^5$，$1 \le r \le n$）表示树的大小与根节点。

对于接下来的 $(n - 1)$ 行，第 $i$ 行输入两个整数 $u_i$ 与 $v_i$（$1 \le u_i, v_i \le n$），表示树上有一条边连接节点 $u_i$ 与 $v_i$。

## 输出格式

输出一行一个整数，表示该树所有可能的 DFS 序中逆序对数量之和。由于答案可能很大，请对 $10^9 + 7$ 取模后输出。

## 样例输入 1

```
5 3
1 5
2 5
3 5
4 3
```

## 样例输出 1

```
24
```

## 样例输入 2

```
10 5
10 2
2 5
10 7
7 1
7 9
4 2
3 10
10 8
3 6
```

## 样例输出 2

```
516
```

## 样例解释

下图展示了样例 1 中的树。

![sample.png](images/9f773308-8849-43b7-b8a4-a026a2c98c34.png)

该树共有 $4$ 种可能的 DFS 序：
* $\{3, 4, 5, 1, 2\}$，有 6 个逆序对；
* $\{3, 4, 5, 2, 1\}$，有 7 个逆序对；
* $\{3, 5, 1, 2, 4\}$，有 5 个逆序对；
* $\{3, 5, 2, 1, 4\}$，有 6 个逆序对。

因此答案为 $6 + 7 + 5 + 6 = 24$。

## 题目详解

### 一、解题思路

每一种 DFS 序只由每个节点的子节点排列顺序决定。设所有可能的 DFS 序数量为 `ways`，则 `ways` 等于所有节点的子节点数阶乘之积。对任意一对节点分别讨论：

- 如果一个节点是另一个节点的祖先，它们在所有 DFS 序中的先后关系固定，直接判断这对节点是否构成逆序对，并乘以 `ways`。
- 如果两个节点位于同一个节点的不同子树中，它们的先后关系只取决于这两棵子树在该节点处的排列顺序。两种相对顺序出现次数相同，因此当节点编号较大的那个排在后面时，这一对在全部 DFS 序中贡献 `ways / 2` 次逆序对。

每一对节点只会在其最近公共祖先处被归入上述情况。可以通过树上 DFS 求出祖先关系，并使用树状数组统计祖先逆序对；再在合并子树节点编号集合时统计不同子树之间的编号大小关系。配合小并大或有序集合合并，整体复杂度可控制在 `O(n log n)`，最后对 `10^9+7` 取模。

### 二、代码流程说明

1. 以 `r` 为根建立父子关系，并统计每个节点的子节点数。
2. 计算 `ways = ∏ factorial(子节点数)`。
3. DFS 遍历树，用树状数组维护当前递归栈中的祖先编号，统计固定先后关系产生的逆序对。
4. 自底向上合并各子树的有序编号集合，统计不同子树之间满足 `u > v` 的节点对。
5. 将两类贡献分别乘以 `ways` 和 `ways / 2`，相加后输出模值。

### 三、复杂度分析

树状数组和子树集合合并均为 `O(n log n)`，空间复杂度为 `O(n)`。
