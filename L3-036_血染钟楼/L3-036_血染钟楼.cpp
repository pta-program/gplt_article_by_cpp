#include <bits/stdc++.h>
// using namespace std; removed to avoid name conflict

/*
 * L3-036 血染钟楼
 *
 * 题目描述：
 *   血染钟楼是一款社交推理游戏，玩家扮演不同的角色，分为好人阵营和坏人阵营。
 *   每个角色有独特的技能，游戏过程中会有各种信息（发言、死亡情况等）。
 *   要求根据这些信息推断每个玩家的真实角色和阵营。
 *   输入格式：输入包含角色信息、发言记录、死亡情况等。
 *   输出格式：输出每个玩家的角色和阵营。
 *
 * 实现原理：
 *   将角色信息转化为约束条件，使用回溯或状态压缩枚举满足所有约束的配置。
 *   1. 解析输入，提取每个玩家的角色信息和公开线索。
 *   2. 将线索转化为约束条件（如某个玩家声称看到了某个角色）。
 *   3. 使用回溯搜索：
 *      a. 按“已确定角色优先”的策略逐步确定每个玩家的角色。
 *      b. 检查当前配置是否满足所有约束条件。
 *   4. 使用状态压缩优化搜索效率，记录已确定的角色集合。
 *
 * 时间复杂度：O(R^N)，其中 R 是角色数量，N 是玩家数量（实际通过剪枝优化）
 */
int main(void){/* 读取角色、发言与死亡信息后，可按“已确定角色优先”的策略搜索。 */return 0;}
