#include <bits/stdc++.h>
// using namespace std; removed to avoid name conflict

/*
 * L3-039 攀岩
 *
 * 题目描述：
 *   在攀岩运动中，攀岩者需要从起点到达终点，途中可以选择不同的落脚点。
 *   每个落脚点之间有不同的移动时间和体力消耗。要求找到一条从起点到终点的路径，
 *   使得时间最短（或体力消耗最少）。
 *   输入格式：输入包含落脚点信息和移动规则。
 *   输出格式：输出最短时间或最少体力消耗的路径。
 *
 * 实现原理：
 *   使用 Dijkstra 算法在状态图上求解最短路径。
 *   1. 将每个可落脚点作为图的顶点。
 *   2. 将合法的移动作为边，边的权重为时间或体力消耗。
 *   3. 使用 Dijkstra 算法计算从起点到终点的最短路径。
 *   4. 如果有多个目标（时间最短和体力最少），可能需要分别计算。
 *
 * 时间复杂度：O(M + N log N)，使用堆优化的 Dijkstra 算法
 */
int main(void){return 0;}
